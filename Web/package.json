{
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:regression": "cypress run --env grepTags=\"@regression --@known-issue\"",
    "cypress:smoke": "cypress run --env grepTags=@smoke",
    "cypress:known-issues": "cypress run --env grepTags=@known-issue"
  },
  "devDependencies": {
    "@cypress/grep": "^6.0.3",
    "cypress": "^15.20.1"
  }
}